# ============================================================================
# SYNERGEX CPANEL PRODUCTION CONFIGURATION
# ============================================================================
# Copy this file content to .env on your cPanel server
# Update all values marked with YOUR_* placeholders

# Database Configuration - UPDATE WITH YOUR CPANEL DATABASE INFO!
DB_HOST=localhost
DB_NAME=YOUR_CPANEL_USERNAME_synergex_db
DB_USER=YOUR_CPANEL_USERNAME_dbuser
DB_PASS=YOUR_SECURE_DATABASE_PASSWORD

# Site Configuration - UPDATE WITH YOUR DOMAIN!
SITE_URL=https://synergexsol.com
APP_ENV=production
APP_DEBUG=false

# WhatsApp Configuration
WHATSAPP_NUMBER=260770377471
WHATSAPP_MESSAGE="Hello Synergex! I would like to know more about your products."

# Email/SMTP Configuration - USE APP PASSWORD FOR GMAIL!
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=synergexsolutions25@gmail.com
SMTP_PASS=YOUR_GMAIL_APP_PASSWORD
SMTP_FROM_EMAIL=synergexsolutions25@gmail.com
SMTP_FROM_NAME="Synergex Solutions"

# Security Settings - KEEP THESE FOR PRODUCTION!
SESSION_LIFETIME=7200
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true

# Timezone
TIMEZONE=Africa/Lusaka

# Upload Configuration
MAX_UPLOAD_SIZE=52428800
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
ALLOWED_VIDEO_TYPES=mp4,webm,ogg,avi,mov

# ============================================================================
# IMPORTANT NOTES FOR CPANEL:
# ============================================================================
# 1. Create database in cPanel → MySQL Databases
# 2. Note the full database name (usually prefixed with your cPanel username)
# 3. Create database user and grant ALL PRIVILEGES
# 4. Get SMTP App Password from Google Account → Security → 2-Step → App Passwords
# 5. Install SSL certificate via cPanel → SSL/TLS before setting SITE_URL to https
# 6. NEVER commit this file to Git! Copy manually to server
# ============================================================================
